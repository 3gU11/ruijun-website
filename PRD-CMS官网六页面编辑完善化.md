# CMS 官网六页面编辑完善化 PRD

## 1. 文档信息

- 版本：1.0
- 日期：2026-09-15
- 项目：瑞俊官网与 CMS 内容编辑工作台
- 状态：待评审

## 2. 背景与问题

当前 CMS 已存在文字、产品参数、图片选择和位置展示相关能力，但实际编辑体验不完整：

1. 后台预览中尺寸图、产品特点图缺少清晰的切换入口，点击可能无反馈。
2. 图片替换、背景图替换、位置调整的可用范围不够明确。
3. 部分字段在后台可见，但未必能保存并同步到官网。
4. 点击预览元素可能影响左侧表单滚动位置。
5. CMS 服务异常时只显示通用错误，难以判断是数据库、权限还是字段错误。

本项目的目标不是开放所有数据库字段，而是让官网六个页面中的实际展示内容具备可理解、可操作、可保存、可验证的编辑能力。

## 3. 产品目标

### 3.1 目标

- 覆盖官网六个页面的核心展示内容。
- 文字、图片、产品结构和布局调整均有明确编辑入口。
- 预览区与编辑表单建立一一对应关系。
- 保存、刷新、官网读取结果保持一致。
- 对不可编辑内容明确说明原因。

### 3.2 非目标

- 本阶段不实现复杂审批流。
- 不开放任意 CSS、HTML、JSON 或数据库字段编辑。
- 不改变官网整体视觉设计。
- 不自动猜测产品图片与型号的关系。
- 不删除或重建现有业务数据。

## 4. 页面范围

1. 首页
2. 产品展示
3. 关于我们
4. 服务支持
5. 新闻资讯
6. 联系我们

官网只展示各页面已配置且满足发布条件的内容；后台可以展示编辑辅助信息，但不应把内部元数据直接渲染到官网。

## 5. 用户与权限

### 内容编辑人员

- 查看六个页面及草稿预览。
- 编辑文字、图片、产品内容和受控位置。
- 保存草稿。
- 不能修改系统字段、权限和任意代码。

### 发布人员

- 具备内容编辑人员全部权限。
- 发布、下线和恢复已发布内容。

本阶段审核流程可以简化，但草稿和发布状态必须保留。

## 6. 功能需求

### FR-01 页面工作台

- 左侧显示六个页面入口和当前状态。
- 页面加载后显示该页面实际可编辑字段。
- 字段按官网区块分组，不按数据库表直接罗列。
- 每个字段显示官网展示位置或区块名称。
- 未保存修改必须有状态标识。

### FR-02 文字编辑

- 支持标题、副标题、正文、按钮文字、产品名称、产品特点文字和尺寸图标题。
- 支持保存、清空和恢复默认值。
- 必填字段、长度限制和格式错误必须在字段附近提示。
- 清空字段必须是明确操作，不得因失焦自动覆盖用户内容。

### FR-03 图片资源编辑

- 支持从媒体库选择已有图片。
- 支持上传新图片并作为当前字段资源。
- 支持替换、移除和恢复默认图片。
- 显示缩略图、文件名、类型、尺寸和引用位置。
- 区分背景图、主图、特点图、尺寸图、新闻图和公共资源。
- 被页面引用的资源不得无提示直接删除。

### FR-04 图片位置编辑

- 支持水平位置和垂直位置调整。
- 提供滑块或数字输入，并即时更新预览。
- 支持恢复默认位置。
- 桌面端与移动端位置可以分别保存；若未设置移动端则使用桌面端默认值。
- 位置值必须经过白名单和范围校验，禁止任意 CSS 注入。

### FR-05 产品展示编辑

- 产品系列支持新增、编辑、隐藏和排序。
- 产品型号支持新增、编辑和隐藏。
- 产品主图支持选择、上传和替换。
- 尺寸参数图支持选择、替换和预览。
- 产品特点支持新增、删除、排序。
- 每个特点独立编辑标题、描述、图片和图片位置。
- 产品字段必须绑定明确的型号或系列，不根据文件名模糊推断。

### FR-06 预览交互

- 点击预览文字时定位对应字段。
- 普通点击不改变左侧当前滚动位置。
- 只有明确的“定位到字段”操作才滚动左侧表单。
- 尺寸图和产品特点图提供可见的标签、缩略图或上一张/下一张按钮。
- 当前预览状态必须显示当前选中的尺寸图或特点图。
- 点击不可编辑元素时显示“该元素暂不可编辑”及原因。

### FR-07 保存与错误处理

- 支持保存当前字段和保存整页草稿。
- 保存成功后刷新预览，不丢失当前选中状态。
- 保存失败显示具体阶段：校验、上传、权限、接口或数据库。
- 网络错误时保留本地未保存内容，提供重试。
- 离开页面前提示未保存修改。
- CMS 不可用时展示服务状态和可执行的重试提示。

### FR-08 发布与官网同步

- 草稿、已发布、已下线状态清晰可见。
- 草稿预览只对授权用户可见。
- 发布后官网读取同一份发布快照。
- 发布后必须触发官网缓存更新或明确提示需要刷新。
- 发布结果应可通过官网实际页面和接口回读验证。

## 7. 数据与接口约束

- 保留现有 Directus 集合和受控 presentation 字段设计。
- 图片字段保存资源 ID 或受控媒体路径，不保存任意外部 URL。
- 位置字段只允许预定义方向或受限数值范围。
- 产品系列、型号、参数、尺寸图、特点图之间必须保持可追溯关联。
- 前端预览和官网展示必须使用一致的字段映射与发布快照。
- 不以修改数据库结构作为第一选择；如确需变更，必须提供迁移和回滚方案。

## 8. 验收标准

### 页面通用验收

- 六个页面均可进入编辑工作台。
- 每页至少完成一项文字、一项图片和一项预览交互测试。
- 保存后刷新 CMS，值保持不变。
- 官网重新读取后展示与已发布数据一致。
- 未保存离开时出现提示。

### 图片验收

- 新图片上传成功并可在媒体库看到。
- 替换后预览立即显示新图片。
- 水平、垂直位置改变后视觉位置确实改变。
- 刷新后位置仍保持。
- 背景图字段若不可替换，必须明确显示原因；若支持，则完成上传、替换、保存、刷新闭环。

### 产品验收

- 点击尺寸图切换入口能看到不同尺寸图。
- 点击产品特点切换入口能看到不同特点图。
- 新增、删除、排序特点后预览与保存结果一致。
- 不同产品型号不会错误复用其他型号图片或参数。

### 稳定性验收

- MySQL 3307、CMS 8055、官网 4175 均可用时完成完整测试。
- CMS 数据库断开时返回可识别错误，不显示无上下文的通用错误。
- 不产生重复产品、重复媒体记录或脏草稿。
- 不修改或删除既有真实业务数据。

## 9. 分阶段开发计划

### Phase 0：运行环境与基线

- 修复并稳定 MySQL 3307、CMS 8055 和官网 4175 启动链路。
- 建立六页面字段映射清单。
- 为每个字段确定预览标识、保存接口和官网读取路径。
- 输出当前功能基线测试结果。

### Phase 1：工作台基础体验

- 完善页面导航、区块分组、字段状态和错误提示。
- 修复预览点击导致左侧表单意外滚动的问题。
- 实现文字字段保存、刷新和回读。

### Phase 2：图片与位置

- 完善媒体库选择和上传入口。
- 完善主图、背景图、特点图和尺寸图映射。
- 实现水平、垂直位置调整及恢复默认。
- 完成桌面端预览验证。

### Phase 3：产品编辑

- 完善产品系列、型号、参数、尺寸图和特点列表编辑。
- 增加尺寸图和特点图的可见切换控件。
- 完成产品关联校验和刷新回读。

### Phase 4：发布与回归

- 完善草稿、发布、下线状态。
- 验证发布快照与官网展示一致。
- 执行六页面、桌面端和移动端回归测试。
- 记录剩余不可编辑字段及后续需求。

## 10. 优先级

- P0：CMS 可用、文字保存、图片替换、位置调整、尺寸图/特点图切换。
- P1：上传体验、撤销、移动端独立位置、发布状态优化。
- P2：批量编辑、历史版本对比、复杂审批和操作审计增强。

## 11. 开发前置条件

1. CMS 健康检查必须返回成功状态。
2. CMS 数据库账号和数据库连接必须稳定。
3. 六页面字段与官网组件映射必须先确认。
4. 现有数据库和媒体资源只允许增量修改，不执行初始化、重建或删除。
5. 每个阶段完成后必须进行接口回读和浏览器实际操作验证。

## 12. 第一轮开发任务清单

- [ ] 修复 CMS 8055 健康检查失败问题。
- [ ] 输出六页面可编辑字段矩阵。
- [ ] 检查预览元素到表单字段的映射完整性。
- [ ] 检查尺寸图和特点图的切换入口与事件处理。
- [ ] 验证图片上传、替换和背景图字段。
- [ ] 验证水平、垂直位置保存及刷新回读。
- [ ] 建立六页面 smoke test。

